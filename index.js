const signupProcessor = library => {
  // Synchronous in nature
  // Done  in any order
};

const bookScanner = (maxScanPerDay, booksToScanArr) => {
  // Asynchronous in nature
  // Track scanned books per day
};
